# Prompt - Start Ribbon 6 Compatibility Implementation

Use this prompt to start the implementation session for `spec/papiflyfx-docking-docks/2026-04-23-0-ribbon-6`.

```text
As @core-architect, lead this PapiflyFX Docking Ribbon 6 compatibility task.

Read these first:
- AGENTS.md
- spec/agents/README.md
- spec/agents/playbook.md
- spec/agents/core-architect.md
- spec/agents/feature-dev.md
- spec/agents/ui-ux-designer.md
- spec/agents/qa-engineer.md
- spec/agents/spec-steward.md
- spec/papiflyfx-docking-docks/ribbon-status.md
- spec/papiflyfx-docking-docks/ribbon-release-notes.md
- spec/papiflyfx-docking-docks/ribbon-provider-authoring.md
- spec/papiflyfx-docking-docks/2026-04-23-0-ribbon-6/README.md
- spec/papiflyfx-docking-docks/2026-04-23-0-ribbon-6/ribbon-6-design.md
- spec/papiflyfx-docking-docks/2026-04-23-0-ribbon-6/plan.md
- spec/papiflyfx-docking-docks/2026-04-23-0-ribbon-6/progress.md

Task:
Implement the Ribbon 6 compatibility plan from `plan.md`, starting with Phase 0. Do not edit Java sources until exact public type names, compatibility bridge/deprecation policy, migration order, and validation scope are recorded in `progress.md`.

Priority:
P2 (Normal)

Implementation lead:
@core-architect

Required reviewers:
@feature-dev, @ui-ux-designer, @qa-engineer, @spec-steward

Consult if scope expands:
@ops-engineer for Maven/TestFX configuration, archetype templates, release automation, publishing, or persisted-session migration tooling.

Impacted modules:
- papiflyfx-docking-api
- papiflyfx-docking-docks
- papiflyfx-docking-github
- papiflyfx-docking-hugo
- papiflyfx-docking-samples
- spec/**

Expected implementation:
1. Phase 0: finalize public type names for action commands, toggle commands, boolean state, subscriptions, control kind, and control strategies/render plans.
2. Phase 0: decide whether `PapiflyCommand`, `BoolState`, and existing control records remain as deprecated compatibility bridges.
3. Phase 1: split command contracts so push/action controls do not carry toggle-only selected state.
4. Phase 1: replace `BoolState` add/remove listener usage with a UI-neutral subscription-returning observable boolean contract and separate mutable state.
5. Phase 1: preserve command registry semantics from Ribbon 5: stable id identity, first metadata, refreshed state, refreshed action dispatch, and diagnostics for incompatible command kinds.
6. Phase 2: move control command extraction, signature comparison, and rendering dispatch toward built-in strategies/render plans while keeping public API UI-neutral.
7. Phase 3: decompose `RibbonManager` into focused package-private collaborators while keeping the public facade stable.
8. Phase 4: update Javadocs, provider-authoring docs, release/migration notes, and `progress.md` validation evidence.

Key invariants:
- Feature modules must remain able to depend on `papiflyfx-docking-api` rather than `papiflyfx-docking-docks`.
- Do not expose JavaFX scene-graph or property types from shared public API contracts.
- Do not add busy/running state to the base command contract in this task.
- Do not add per-user customization UI/schema, keytips, or galleries unless a separate design is created and approved.
- Keep persisted ribbon state under `extensions.ribbon`.
- Keep QAT persistence as `quickAccessCommandIds`; do not persist command objects.
- Unknown ribbon session fields are ignored on decode; malformed known fields remain strict and isolated to ribbon extension restore.
- Arbitrary JavaFX node factories from feature modules are out of scope.

Validation expectations:
- `./mvnw -pl papiflyfx-docking-api,papiflyfx-docking-docks -am compile`
- `./mvnw -pl papiflyfx-docking-docks -am -Dtest=Ribbon*Test,CommandRegistryTest -Dsurefire.failIfNoSpecifiedTests=false -Dtestfx.headless=true test`
- `./mvnw -pl papiflyfx-docking-github,papiflyfx-docking-hugo -am -Dtestfx.headless=true test`
- `./mvnw -pl papiflyfx-docking-samples -am -Dtest=*Ribbon*FxTest -Dsurefire.failIfNoSpecifiedTests=false -Dtestfx.headless=true test`
- `git diff --check`

Acceptance criteria:
- Action-only and toggle-capable ribbon command contracts are separated and documented.
- Toggle selection state is unavailable on action-only commands after migration.
- Boolean state listener lifecycle is subscription-based and disposal is covered by tests.
- Runtime diagnostics catch a command id contributed as incompatible command kinds.
- Existing built-in controls render equivalently after strategy/render-plan migration.
- Unknown control kinds are diagnosed and skipped without aborting the ribbon refresh.
- `RibbonManager` public facade remains stable while package-private collaborators own provider discovery, tab merging, command canonicalization, QAT state, and refresh scheduling.
- Provider-authoring docs and release/migration notes explain all public breaks.
- `progress.md` records phase status, files changed, validation commands/results, deferred work, and reviewer handoff.

Close with:
1. Files changed
2. Validation commands and results
3. Public compatibility breaks and migration notes
4. Remaining risks and deferred items
5. Handoff block using the repository handoff contract
```
