# Prompt - Start Ribbon 5 Phase 2 Implementation

Use this prompt to start the next implementation session for `spec/papiflyfx-docking-docks/2026-04-23-0-ribbon-5`.

```text
As @core-architect with @feature-dev support, lead Phase 2 of the Ribbon 5 consolidated follow-up.

Read these first:
- AGENTS.md
- spec/agents/README.md
- spec/agents/playbook.md
- spec/agents/core-architect.md
- spec/agents/feature-dev.md
- spec/agents/qa-engineer.md
- spec/agents/spec-steward.md
- spec/agents/ops-engineer.md
- spec/papiflyfx-docking-docks/2026-04-23-0-ribbon-5/README.md
- spec/papiflyfx-docking-docks/2026-04-23-0-ribbon-5/plan.md
- spec/papiflyfx-docking-docks/2026-04-23-0-ribbon-5/progress.md
- spec/papiflyfx-docking-docks/2026-04-23-0-ribbon-5/review-core-architect.md
- spec/papiflyfx-docking-docks/2026-04-23-0-ribbon-5/review-feature-dev.md
- spec/papiflyfx-docking-docks/2026-04-23-0-ribbon-5/review-qa-engineer.md
- spec/papiflyfx-docking-docks/2026-04-23-0-ribbon-5/review-ops-engineer.md
- spec/papiflyfx-docking-docks/2026-04-23-0-ribbon-5/review-spec-steward.md

Task:
Implement Phase 2 - Context And Provider Authoring Foundation from the Ribbon 5 plan.

Baseline:
Phase 1 is complete. Preserve its decisions unless a regression proves they are wrong:
- CommandRegistry projects incoming enabled/selected state into canonical commands.
- JavaFX command bindings are disposable and listener counts stay bounded.
- Provider failures and duplicate tab/command ids emit telemetry/warnings.
- Collapsed group popups restore focus to their trigger.
- Ribbon focus/accessibility/disabled-icon/theme behavior has focused coverage.

Priority:
P2 (Medium), after Phase 1 reviewer feedback is resolved or explicitly recorded.

Implementation lead:
@core-architect

Support lead:
@feature-dev

Required reviewers:
@feature-dev, @qa-engineer, @spec-steward, @ops-engineer

Primary impacted modules:
- papiflyfx-docking-api
- papiflyfx-docking-docks
- papiflyfx-docking-hugo
- spec/**

Secondary modules may be touched only if validation, authoring guidance, or scaffold decisions require it:
- papiflyfx-docking-github
- papiflyfx-docking-samples
- papiflyfx-docking-archetype

Mandatory Phase 2 scope:
1. Design and implement a non-breaking typed context metadata path.
   - Source findings: review-core-architect F-06, review-feature-dev F-03, review-spec-steward F-05.
   - Prefer a typed key abstraction such as `RibbonAttributeKey<T>` plus overloads on `RibbonContext`.
   - Keep existing raw-string attribute access compatible for this phase.
   - Document attribute namespacing, ownership, and migration guidance.
   - Add canonical keys for existing `RibbonContextAttributes` values where practical.
   - Add explicit contextual metadata keys for content kind/domain if they are needed to replace provider heuristics.

2. Define explicit capability contribution semantics.
   - Source findings: review-core-architect F-10, review-feature-dev F-04, review-qa-engineer F-01.
   - Decide and implement one non-breaking strategy:
     - document root-node `implements ActionInterface` as the supported convention and tighten tests/docs around it, or
     - add a small API-level capability contribution contract that lets content publish one or more explicit capabilities.
   - Keep feature modules able to depend on `papiflyfx-docking-api`, not `papiflyfx-docking-docks`.
   - Cover behavior when the active content is in a floating window.

3. Write provider-authoring guidance before adding broad helpers.
   - Source findings: review-feature-dev F-01/F06/F-07, review-ops-engineer F-01, review-spec-steward F-05.
   - Document provider id and command id namespace conventions.
   - Document ServiceLoader registration, tab/group/command construction, icon conventions, capability lookup, contextual metadata, QAT implications, and test checklist.
   - Add provider helper APIs only if the guide exposes stable repeated patterns that are small, non-breaking, and clearly worth centralizing.

4. Improve Hugo contextual-tab activation coverage and migration path.
   - Source findings: review-feature-dev F-03, review-qa-engineer F-05.
   - Add table-style tests for each legacy heuristic branch:
     - active type key
     - `CONTENT_FACTORY_ID`
     - `.md` / `.markdown` file extension
     - `/content/` path segment
     - type-key text containing markdown or hugo
     - negative/non-Hugo content
   - If typed metadata keys are introduced, make Hugo prefer explicit metadata and keep documented legacy heuristics as fallback.

5. Add focused floating-window ribbon context coverage.
   - Source findings: review-qa-engineer F-01, review-feature-dev F-04.
   - Add a `papiflyfx-docking-docks` Fx test that builds a `RibbonDockHost`, activates a capability-bearing leaf in a floating stage, and asserts:
     - the ribbon context identifies the active floating content
     - `RibbonContextAttributes.FLOATING` or its typed-key equivalent is true
     - the provider capability resolves
     - the relevant tab/command state updates

6. Decide whether docs alone are enough or whether an archetype/sample scaffold is required now.
   - Source finding: review-ops-engineer F-01.
   - If docs are enough for Phase 2, record the archetype scaffold as a deferred Phase 3/5 item with owner and reason.
   - If a scaffold is required, keep it minimal: `RibbonDockHost`, one trivial provider, ServiceLoader descriptor, and one generated-app assertion.

Non-goals for this phase:
- Do not implement Ribbon 6 breaking API redesigns.
- Do not remove raw-string context attributes or existing `RibbonContextAttributes` constants in Phase 2.
- Do not remove Hugo legacy heuristics until a migration plan and compatibility note exist.
- Do not implement code/tree/media production ribbon providers.
- Do not add per-user customization, keytips, galleries, command busy state, or session schema changes.
- Do not centralize Maven/TestFX flags or add performance benchmarks unless directly required by Phase 2 validation.

Key invariants:
- Feature modules must remain able to depend on `papiflyfx-docking-api`, not `papiflyfx-docking-docks`.
- QAT id-first persistence semantics must remain unchanged.
- Phase 1 command-state projection, binding disposal, and diagnostics behavior must remain green.
- Contextual tab behavior must become more explicit without breaking current GitHub/Hugo/sample behavior.
- Public API additions require Javadoc and spec notes; public API removals are out of scope.
- Update `progress.md` as each Phase 2 milestone completes.

Recommended implementation order:
1. Inspect current `RibbonContext`, `RibbonContextAttributes`, `DockManager.buildRibbonContext`, existing provider tests, and Hugo heuristic code.
2. Add focused tests for typed metadata compatibility, explicit capability semantics, Hugo heuristic branches, and floating-window context.
3. Implement the smallest non-breaking API/runtime changes needed for typed metadata and capability contribution.
4. Update Hugo provider behavior only after tests pin the legacy fallback behavior.
5. Add or update provider-authoring docs and decide whether archetype/sample scaffold work is required now.
6. Run focused validation, then broaden only for touched provider/archetype/sample modules.
7. Update `progress.md` with files changed, validation commands/results, deferrals, and residual risks.

Validation expectations:
- `./mvnw -pl papiflyfx-docking-api,papiflyfx-docking-docks -am compile`
- `./mvnw -pl papiflyfx-docking-docks -am '-Dtest=Ribbon*Test,Ribbon*FxTest,CommandRegistryTest' -Dsurefire.failIfNoSpecifiedTests=false -Dtestfx.headless=true test`
- If Hugo provider behavior changes:
  - `./mvnw -pl papiflyfx-docking-hugo -am -Dtest=HugoRibbonProviderTest -Dsurefire.failIfNoSpecifiedTests=false -Dtestfx.headless=true test`
- If provider behavior or capability contracts change:
  - `./mvnw -pl papiflyfx-docking-github,papiflyfx-docking-hugo -am -Dtestfx.headless=true test`
- If samples are touched:
  - `./mvnw -pl papiflyfx-docking-samples -am -Dtest=SamplesSmokeTest -Dsurefire.failIfNoSpecifiedTests=false -Dtestfx.headless=true test`
  - `./mvnw -pl papiflyfx-docking-samples -am '-Dtest=*Ribbon*FxTest' -Dsurefire.failIfNoSpecifiedTests=false -Dtestfx.headless=true test`
- If archetype templates are touched:
  - `./mvnw -pl papiflyfx-docking-bom,papiflyfx-docking-archetype -am clean install`

Acceptance criteria:
- A new feature module can follow documented steps to contribute a ribbon provider without reading runtime internals.
- Contextual tab activation has an explicit metadata path, with Hugo legacy heuristics clearly documented and tested as fallback.
- Capability contribution and lookup rules are documented and covered, including floating-window active content.
- API additions are non-breaking, documented with Javadoc, and reflected in the spec/progress notes.
- No Phase 1 regression occurs in command state, listener lifecycle, provider diagnostics, QAT persistence, or accessibility behavior.
- `progress.md` records completed work, validation commands/results, deferrals, and remaining risks.

Close with:
1. Files changed
2. Validation commands and results
3. Findings closed or deferred
4. Remaining risks
5. Handoff block using the repository handoff contract
```
