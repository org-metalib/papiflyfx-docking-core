# Prompt - Start Ribbon 5 Implementation

Use this prompt to start the implementation session for `spec/papiflyfx-docking-docks/2026-04-23-0-ribbon-5`.

```text
As @core-architect, lead Phase 1 of the Ribbon 5 consolidated follow-up.

Read these first:
- AGENTS.md
- spec/agents/README.md
- spec/agents/playbook.md
- spec/agents/core-architect.md
- spec/agents/ui-ux-designer.md
- spec/agents/qa-engineer.md
- spec/papiflyfx-docking-docks/2026-04-23-0-ribbon-5/README.md
- spec/papiflyfx-docking-docks/2026-04-23-0-ribbon-5/plan.md
- spec/papiflyfx-docking-docks/2026-04-23-0-ribbon-5/progress.md
- spec/papiflyfx-docking-docks/2026-04-23-0-ribbon-5/review-core-architect.md
- spec/papiflyfx-docking-docks/2026-04-23-0-ribbon-5/review-feature-dev.md
- spec/papiflyfx-docking-docks/2026-04-23-0-ribbon-5/review-ui-ux-designer.md
- spec/papiflyfx-docking-docks/2026-04-23-0-ribbon-5/review-qa-engineer.md

Task:
Implement Phase 1 - P1 Runtime And Accessibility Stabilization from the Ribbon 5 plan.

Priority:
P1 (High)

Implementation lead:
@core-architect

Required reviewers:
@ui-ux-designer, @qa-engineer, @feature-dev, @spec-steward

Primary impacted modules:
- papiflyfx-docking-api
- papiflyfx-docking-docks
- spec/**

Secondary modules may be touched only if validation or provider compatibility requires it:
- papiflyfx-docking-github
- papiflyfx-docking-hugo
- papiflyfx-docking-samples

Mandatory Phase 1 scope:
1. Fix command-state canonicalization semantics.
   - Source findings: review-core-architect F-01, review-feature-dev F-02.
   - Decide and implement one strategy:
     - project incoming command state into canonical commands, or
     - introduce provider-side command state helpers.
   - Add regression coverage proving a command first materialized disabled can become enabled after context/provider refresh.

2. Fix JavaFX command binding listener lifecycle.
   - Source finding: review-core-architect F-02.
   - Make `JavaFxCommandBindings.readOnly(...)` and `bidirectional(...)` disposable, or otherwise prove listener counts stay bounded.
   - Add a focused regression using `MutableBoolState.listenersSize()` or an equivalent observable bound.

3. Add provider and id-collision diagnostics.
   - Source findings: review-core-architect F-04/F-09, review-feature-dev F-07, review-qa-engineer F-02.
   - Add test-observable diagnostics for provider failures.
   - Add warning/telemetry coverage for conflicting tab or command ids where first-wins behavior applies.
   - Document first-wins or conflict behavior in API/runtime docs where contributors will find it.

4. Improve collapsed-popup keyboard behavior.
   - Source findings: review-ui-ux-designer F-04, review-qa-engineer F-04.
   - Restore focus to the collapsed group trigger when the popup hides.
   - Add TestFX coverage for Escape dismissal and focus return.

5. Improve ribbon accessibility and control states.
   - Source findings: review-ui-ux-designer F-03/F-05/F-06/F-07 and review-qa-engineer F-03.
   - Add visible focus states for ribbon tabs, QAT buttons, group launchers, and collapsed-group buttons.
   - Set accessible text for icon-only ribbon controls.
   - Add disabled icon styling for SVG/octicon controls and decide raster icon behavior.
   - Add contextual selected/focused styling using theme-derived accent tokens.
   - Add or extend theme-switching coverage for live ribbon chrome and collapsed popups.

Non-goals for this kickoff:
- Do not implement broad Ribbon 6 breaking API redesigns (`PapiflyCommand` split, non-sealed controls, full `RibbonManager` extraction).
- Do not implement per-user customization.
- Do not start archetype/provider-authoring docs unless a Phase 1 change requires a small doc note.
- Do not change public API/session contracts without updating the spec and recording migration notes.

Key invariants:
- Keep feature modules able to depend on `papiflyfx-docking-api`, not `papiflyfx-docking-docks`.
- Preserve QAT id-first persistence semantics.
- Preserve current ribbon provider discovery and contextual tab behavior unless a test proves the old behavior was defective.
- Keep visual changes in the shared `-pf-ui-*` token vocabulary and scoped ribbon CSS.
- Update `progress.md` as each Phase 1 milestone completes.

Recommended implementation order:
1. Add failing tests for command-state refresh and listener lifecycle.
2. Implement command-state and binding fixes.
3. Add provider failure/collision diagnostics and tests.
4. Add popup focus/accessibility CSS/control updates and tests.
5. Run focused validation, then broaden to provider/sample compile or tests as needed.
6. Update `progress.md` with files changed, validation, and residual risks.

Validation expectations:
- `./mvnw -pl papiflyfx-docking-api,papiflyfx-docking-docks -am compile`
- `./mvnw -pl papiflyfx-docking-docks -am '-Dtest=Ribbon*Test,CommandRegistryTest' -Dsurefire.failIfNoSpecifiedTests=false -Dtestfx.headless=true test`
- If provider behavior or API docs change:
  - `./mvnw -pl papiflyfx-docking-github,papiflyfx-docking-hugo -am compile`
- If theme/sample behavior changes:
  - `./mvnw -pl papiflyfx-docking-samples -am '-Dtest=*Ribbon*FxTest' -Dsurefire.failIfNoSpecifiedTests=false -Dtestfx.headless=true test`

Acceptance criteria:
- Command enabled/selected state refreshes correctly across provider/context refresh.
- Canonical command listener counts stay bounded across refresh/rebuild cycles.
- A failing provider cannot break healthy provider tabs and emits a test-observable diagnostic.
- Duplicate id behavior is documented and covered by warning/telemetry tests.
- Collapsed group popup closes by keyboard and returns focus to its trigger.
- Focus, accessible-name, disabled icon, contextual tab, and theme-switching behavior match the UI/UX findings.
- `progress.md` records completed work, validation commands/results, and remaining risks.

Close with:
1. Files changed
2. Validation commands and results
3. Findings closed or deferred
4. Remaining risks
5. Handoff block using the repository handoff contract
```
