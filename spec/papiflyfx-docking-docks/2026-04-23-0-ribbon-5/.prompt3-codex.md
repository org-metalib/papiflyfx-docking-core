# Prompt - Start Ribbon 5 Phase 3 Implementation

Use this prompt to start the next implementation session for `spec/papiflyfx-docking-docks/2026-04-23-0-ribbon-5`.

```text
As @qa-engineer with @ops-engineer support, lead Phase 3 of the Ribbon 5 consolidated follow-up.

Read these first:
- AGENTS.md
- spec/agents/README.md
- spec/agents/playbook.md
- spec/agents/qa-engineer.md
- spec/agents/ops-engineer.md
- spec/agents/core-architect.md
- spec/agents/ui-ux-designer.md
- spec/agents/spec-steward.md
- spec/papiflyfx-docking-docks/2026-04-23-0-ribbon-5/README.md
- spec/papiflyfx-docking-docks/2026-04-23-0-ribbon-5/plan.md
- spec/papiflyfx-docking-docks/2026-04-23-0-ribbon-5/progress.md
- spec/papiflyfx-docking-docks/2026-04-23-0-ribbon-5/review-qa-engineer.md
- spec/papiflyfx-docking-docks/2026-04-23-0-ribbon-5/review-ops-engineer.md
- spec/papiflyfx-docking-docks/2026-04-23-0-ribbon-5/review-core-architect.md
- spec/papiflyfx-docking-docks/2026-04-23-0-ribbon-5/review-ui-ux-designer.md
- spec/papiflyfx-docking-docks/2026-04-23-0-ribbon-5/review-spec-steward.md
- spec/papiflyfx-docking-docks/ribbon-provider-authoring.md

Task:
Implement Phase 3 - Build, Fixture, And Performance Guardrails from the Ribbon 5 plan.

Baseline:
Phase 1 and Phase 2 are complete. Preserve their decisions unless a regression proves they are wrong:
- CommandRegistry projects incoming enabled/selected state into canonical commands.
- JavaFX command bindings are disposable and listener counts stay bounded.
- Provider failures and duplicate tab/command ids emit telemetry/warnings.
- Collapsed group popups restore focus to their trigger.
- Ribbon focus/accessibility/disabled-icon/theme behavior has focused coverage.
- RibbonContext has typed `RibbonAttributeKey<T>` metadata while preserving raw-string attributes.
- Active content can publish metadata/capabilities through `RibbonAttributeContributor` and `RibbonCapabilityContributor`.
- DockManager resolves floating active content and sets `FLOATING_KEY`.
- Hugo contextual tabs prefer explicit content metadata and keep documented legacy heuristics as fallback.
- Provider-authoring guidance exists and archetype scaffold work is deferred.

Priority:
P2 (Medium), after Phase 2 reviewer feedback is resolved or explicitly recorded.

Implementation lead:
@qa-engineer

Support lead:
@ops-engineer

Required reviewers:
@ops-engineer, @core-architect, @ui-ux-designer, @spec-steward

Primary impacted modules:
- papiflyfx-docking-docks
- pom.xml and module POMs only if TestFX/Surefire centralization is implemented
- spec/**

Secondary modules may be touched only if fixture adoption, focused-run documentation, or validation requires it:
- papiflyfx-docking-github
- papiflyfx-docking-hugo
- papiflyfx-docking-samples
- papiflyfx-docking-archetype

Mandatory Phase 3 scope:
1. Decide whether common TestFX/Surefire flags should be centralized now or documented only.
   - Source findings: review-qa-engineer F-08, review-ops-engineer F-02.
   - If centralizing POM config, keep module-specific native-access/module flags intact and run broader validation.
   - If documenting only, update the relevant spec/provider-authoring docs with focused selector examples using `-Dsurefire.failIfNoSpecifiedTests=false`.
   - Do not churn Maven configuration unless there is a clear reduction in duplication without weakening module-specific JavaFX requirements.

2. Introduce shared ribbon test support only where duplication is now proven.
   - Source finding: review-qa-engineer F-07.
   - Inspect Phase 1 and Phase 2 test additions before designing the helper.
   - Keep helpers test-scoped and narrow: in-memory providers, `RibbonDockHost` setup, context changes, command lookup, and FX settle helpers are candidates.
   - Avoid broad fixture frameworks or test abstractions that obscure behavior under test.

3. Add or explicitly defer rapid context churn coverage.
   - Source finding: review-qa-engineer F-06.
   - Prefer a deterministic Fx test that queues multiple context changes in a single FX turn and asserts final tab visibility, selected tab, and command registry state.
   - If implementation behavior is already adequately covered or needs a runtime coalescing design first, record the deferral with owner and reason.

4. Decide on provider-module Fx coverage after sample-level coverage is considered.
   - Source finding: review-qa-engineer F-10.
   - Add module-local Fx tests only where they catch behavior not already covered by samples or unit tests.
   - Candidate coverage: mounted Hugo/GitHub provider visibility flips and capability enablement.
   - Keep feature module production dependencies pointed at `papiflyfx-docking-api`, not `papiflyfx-docking-docks`.

5. Decide whether an opt-in ribbon refresh/adaptive-layout benchmark is useful now.
   - Source findings: review-qa-engineer F-09, review-core-architect F-11.
   - Do not add timing assertions or default-CI performance tests without a concrete budget.
   - If deferred, record the needed budget, owner, and likely benchmark shape.
   - If implemented, tag it as `benchmark` or otherwise keep it excluded from default CI.

6. Update phase tracking.
   - Update `progress.md` as milestones complete.
   - Record exact validation commands, results, deferrals, and residual risks.
   - If POM/test infrastructure changes are made, explain why docs-only was not sufficient.

Non-goals for this phase:
- Do not implement Ribbon 6 breaking API redesigns.
- Do not remove raw-string attributes, `RibbonContextAttributes` constants, or Hugo legacy heuristics.
- Do not implement code/tree/media production ribbon providers.
- Do not add per-user customization, keytips, galleries, command busy state, or session schema changes.
- Do not add broad archetype ribbon scaffold work unless Phase 3 explicitly reopens the Phase 2 deferral and @ops-engineer owns the generated-app validation.

Key invariants:
- Feature modules must remain able to depend on `papiflyfx-docking-api`, not `papiflyfx-docking-docks`.
- QAT id-first persistence semantics must remain unchanged.
- Phase 1 command-state, listener lifecycle, diagnostics, focus, accessibility, disabled-icon, and theme-switch coverage must remain green.
- Phase 2 typed metadata, explicit capability contribution, Hugo fallback behavior, and floating-window active content coverage must remain green.
- Test changes should improve determinism and feedback loops without making default CI materially heavier.
- Public API/session changes are out of scope.

Recommended implementation order:
1. Inspect the current ribbon test classes and Phase 2 additions, especially `RibbonFloatingContextFxTest`, `RibbonManagerTest`, `RibbonAdaptiveLayoutFxTest`, `RibbonCommandRegistryFxTest`, and Hugo/GitHub provider tests.
2. Decide docs-only vs POM centralization for focused TestFX/Surefire flags.
3. Extract a small test helper only if two or more tests clearly benefit without hiding important behavior.
4. Add rapid context churn and any justified provider-module Fx coverage.
5. Decide benchmark deferral vs opt-in implementation.
6. Run focused validation first, then broaden if Maven/test infrastructure changed.
7. Update `progress.md` with files changed, validation commands/results, deferrals, and remaining risks.

Validation expectations:
- Always run:
  - `./mvnw -pl papiflyfx-docking-docks -am '-Dtest=Ribbon*Test,Ribbon*FxTest,CommandRegistryTest' -Dsurefire.failIfNoSpecifiedTests=false -Dtestfx.headless=true test`
- If POM/TestFX/Surefire config changes:
  - `./mvnw -pl papiflyfx-docking-github,papiflyfx-docking-hugo,papiflyfx-docking-samples -am -Dtestfx.headless=true test`
  - Consider `./mvnw -Dtestfx.headless=true test` only if parent/global configuration changed.
- If provider-module Fx coverage changes:
  - `./mvnw -pl papiflyfx-docking-github,papiflyfx-docking-hugo -am -Dtestfx.headless=true test`
- If samples are touched:
  - `./mvnw -pl papiflyfx-docking-samples -am -Dtest=SamplesSmokeTest -Dsurefire.failIfNoSpecifiedTests=false -Dtestfx.headless=true test`
  - `./mvnw -pl papiflyfx-docking-samples -am '-Dtest=*Ribbon*FxTest' -Dsurefire.failIfNoSpecifiedTests=false -Dtestfx.headless=true test`
- If archetype templates are touched:
  - `./mvnw -pl papiflyfx-docking-bom,papiflyfx-docking-archetype -am clean install`

Acceptance criteria:
- Focused ribbon test commands are documented or made reliable through build configuration.
- Any shared ribbon test helper is small, test-scoped, and demonstrably reduces repeated fixture code.
- Rapid context churn is covered or explicitly deferred with a concrete owner/reason.
- Provider-module Fx coverage is added only where it improves signal beyond sample-level coverage.
- Any benchmark work is opt-in and excluded from default CI unless a release gate is explicitly approved.
- Phase 1 and Phase 2 behavior remains green in focused validation.
- `progress.md` records completed work, validation commands/results, deferrals, and remaining risks.

Close with:
1. Files changed
2. Validation commands and results
3. Findings closed or deferred
4. Remaining risks
5. Handoff block using the repository handoff contract
```
